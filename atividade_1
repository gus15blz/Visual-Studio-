using System;
using System.Collections.Generic;
using System.Data;
using System.Diagnostics.Eventing.Reader;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace atividade1
{
    internal class Program
    {
        static void Main(string[] args)
        {
            var idade = 16;

            if (idade >= 18) ;
            {
                Console.WriteLine("Ja pode tirar a Habilitação");
            }

            if (idade <= 17)
            {
                Console.WriteLine("Ainda não pode tirar a Habilitação");
            }
           
            Console.ReadLine();
        }
    }
}
